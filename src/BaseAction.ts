export class BaseAction {
  onActivate() {}
}

/*
 * hooks/action needed
 *
 * damageActive(amount, element)
 * summon(summon)
 * healActive(amount)
 * healAll(amount)
 * healTarget(monster)
 * selectMonster(player | opponent | both, userSelect, active?)
 * useSkill(skill)
 * onDamageCalct
 *
 * Barbara's talent card is a bit rough cause it changes die requirements
 *
 */
